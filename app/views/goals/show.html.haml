- model_class = Goal

- if @goal
  %p
    %h2.lead
      = @goal.description
  %p
    %strong= model_class.human_attribute_name(:quantity) + ':'
    %br
    = @goal.quantity

  = link_to t('.edit', :default => t("helpers.links.edit")), edit_goals_path, :class => 'btn btn-default'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), goals_path, :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'

- else
  .alert.alert-box.alert-info
    You haven't set your goal yet. Hurry up 
    = link_to 'and set your goal', new_goals_path
    now to get started on making 2014 great!