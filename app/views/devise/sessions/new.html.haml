%h2 Sign in with your account...


.row
  .col-md-3
    = link_to "#{content_tag(:h2, 'Google')}#{glyph('google-plus')}".html_safe, user_omniauth_authorize_path(provider: :google_oauth2), class: 'xxl'
  .col-md-3
    = link_to "#{content_tag(:h2, 'Twitter')}#{glyph('twitter')}".html_safe, user_omniauth_authorize_path(provider: :twitter), class: 'xxl'
  .col-md-3
    = link_to "#{content_tag(:h2, 'Facebook')}#{glyph('facebook')}".html_safe, user_omniauth_authorize_path(provider: :facebook), class: 'xxl'



/<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
/  <div class="form-inputs">
/    <%= f.input :email, :required => false, :autofocus => true %>
/    <%= f.input :password, :required => false %>
/    <%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
/  </div>
/  <div class="form-actions">
/    <%= f.button :submit, "Sign in" %>
/  </div>
/<% end %>

/<%= render "devise/shared/links" %>
